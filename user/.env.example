# (선택) 키 목록 초안·메모용 — 실제 로드는 web/.env.local
# web/.env.example 과 동일 키를 맞추고, 값은 web/.env.local에만 둔다.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
MQTT_BROKER_URL=
MQTT_USERNAME=
MQTT_PASSWORD=
