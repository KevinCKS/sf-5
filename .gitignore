# 저장소 루트 — git 초기화 시 시크릿 누락 방지
**/node_modules
**/.next
**/out
**/.env.local
**/.env*.local
.env
!**/.env.example

# Arduino — 로컬에만 두는 WiFi/MQTT 비밀 (예시는 arduino_secrets.h.example 커밋)
user/script/**/arduino_secrets.h
